package engine.repository;

import engine.domain.ActualAnswer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ActualAnswerRepository extends JpaRepository<ActualAnswer, Long> {
}
